name: sra_search
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python>=3.10
  - biopython>=1.83
  - pandas>=2.0
  - click>=8.0
  - aiohttp>=3.9
  - loguru>=0.7
  - pydantic>=2.0
  - requests>=2.28.0
  - rich>=13.0.0
  - pip
  - pip:
    - pydantic-settings>=2.0
    - pysradb>=2.0        # 可选：SRA 原始序列下载
    - GEOparse>=2.0       # 可选：GEO 数据解析
    # 开发依赖（取消注释以启用）
    # - pytest>=7.0
    # - pytest-asyncio>=0.21
    # - pytest-cov>=4.0
    # - black>=23.0.0
    # - ruff>=0.0.270
    # - mypy>=1.0.0
