name: SRA_search
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.10
  - biopython>=1.83
  - pandas>=2.0
  - click>=8.0
  - aiohttp>=3.9
  - loguru>=0.7
  - pydantic>=2.0
  - pydantic-settings>=2.0
  - pip
  - pip:
    - pysradb>=2.0
    - GEOparse>=2.0
